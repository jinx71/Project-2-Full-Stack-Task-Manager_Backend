# PostgreSQL connection string (Railway/Render provide this, or local: postgresql://user:pass@localhost:5432/taskmanager)
DATABASE_URL="postgresql://neondb_owner:npg_CbcKe2oPpw5t@ep-fragrant-surf-at0mox0h-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# JWT signing secret — generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET="replace-with-a-long-random-string"
JWT_EXPIRES_IN="7d"

# Server
PORT=5000

# Allowed frontend origin for CORS
CLIENT_URL="http://localhost:5173"
